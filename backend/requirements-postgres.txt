-r requirements.txt
# psycopg (v3) - actively maintained, ships prebuilt wheels for current Python
# versions. Django's postgres backend auto-detects either this or psycopg2,
# no settings.py changes needed either way.
psycopg[binary]==3.2.3
